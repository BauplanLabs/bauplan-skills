[project]
name = "bauplan-skills"
version = "2.1.0"
description = "Claude Code skills for Bauplan data lakehouse workflows"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bauplan>=0.1.16",
]
